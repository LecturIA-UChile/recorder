# =============================================================================
# Kiro local configuration. Steering and settings are local to each developer
# and MUST NOT be committed.
# =============================================================================
.kiro/

# =============================================================================
# PII safety net. These patterns match local user data so it cannot be
# committed to the repository by accident.
# =============================================================================
**/names.json
**/estudiantes*.json
**/lista_*.json
**/LecturIA_grabaciones/
**/Grabaciones LecturIA/
*.wav
*.mp3
*.lra
# Allow example fixtures explicitly:
!**/names.example.json

# =============================================================================
# Cryptographic key material. PEM files are ignored by default; the only
# committed public key is allowed below.
# =============================================================================
*.pem
!**/public-key.pem
*.p12
*.pfx
*.key

# Application UID key. Regenerate with the keys tool on each developer
# machine and ship as a build secret in CI. NEVER commit.
**/uid-key.txt

# Ed25519 admin private keys live here (one per distributor). The *.key
# pattern in the crypto section already blocks them; this blocks the whole
# scratch folder too. Only the public keys in admin-public-keys.txt are
# committed. Never commit the private keys.
.keys-local/

# =============================================================================
# .NET build outputs
# =============================================================================
[Bb]in/
[Oo]bj/
[Pp]ublish/
[Dd]ist/
*.user
*.userosscache
*.suo
*.sln.docstates

# =============================================================================
# IDE caches
# =============================================================================
.vs/
.idea/
.vscode/
*.iml

# =============================================================================
# NuGet
# =============================================================================
*.nupkg
*.snupkg
project.lock.json
project.fragment.lock.json
artifacts/

# =============================================================================
# Symbols and tests
# =============================================================================
*.pdb
[Tt]est[Rr]esult*/
coverage*.xml
coverage*.json

# =============================================================================
# Legacy Python artifacts
# =============================================================================
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# =============================================================================
# OS metadata
# =============================================================================
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# =============================================================================
# Logs
# =============================================================================
*.log

# =============================================================================
# Developer-only Markdown notes. Anything ending in .local.md is treated as
# personal scratch space and never committed.
# =============================================================================
*.local.md

# =============================================================================
# Thesis working notes. Internal design/audit documentation that supports the
# thesis but must not ship with the repository.
# =============================================================================
thesis/