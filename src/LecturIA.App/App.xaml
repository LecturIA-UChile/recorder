<Application x:Class="LecturIA.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnLastWindowClose">
    <Application.Resources>
        <ResourceDictionary>
            <SolidColorBrush x:Key="AccentBrush" Color="#1E88E5"/>
            <SolidColorBrush x:Key="RecordBrush" Color="#43A047"/>
            <SolidColorBrush x:Key="StopBrush" Color="#E53935"/>
            <SolidColorBrush x:Key="BackgroundBrush" Color="#F5F7FA"/>
            <SolidColorBrush x:Key="TextBrush" Color="#1F2933"/>
            <SolidColorBrush x:Key="MutedBrush" Color="#52606D"/>

            <Style TargetType="Button" x:Key="PrimaryButton">
                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Padding" Value="14,8"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border CornerRadius="6"
                                    Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="0.5"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="Button" x:Key="RecordButton" BasedOn="{StaticResource PrimaryButton}">
                <Setter Property="Background" Value="{StaticResource RecordBrush}"/>
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="Padding" Value="20,12"/>
                <Setter Property="MinWidth" Value="200"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
