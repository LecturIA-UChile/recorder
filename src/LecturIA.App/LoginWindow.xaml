<Window x:Class="LecturIA.App.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:LecturIA.App.ViewModels"
        mc:Ignorable="d"
        Title="Acceso de distribuidor"
        Height="460" Width="540"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource TextBrush}"
        d:DataContext="{d:DesignInstance Type=vm:LoginViewModel}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Acceso de distribuidor"
                   FontSize="22"
                   FontWeight="Bold"
                   Foreground="{DynamicResource TextBrush}"/>

        <TextBlock Grid.Row="1"
                   Text="Pega tu token de distribuidor para habilitar los controles de importación y eliminación de cursos. El token está firmado con tu clave privada y se verifica contra la clave pública embebida en la aplicación."
                   FontSize="13"
                   Foreground="{DynamicResource MutedBrush}"
                   TextWrapping="Wrap"
                   Margin="0,8,0,12"/>

        <Border Grid.Row="2"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Background="{DynamicResource SurfaceBrush}">
            <TextBox Text="{Binding Token, UpdateSourceTrigger=PropertyChanged}"
                     IsEnabled="{Binding IsIdle}"
                     FontFamily="Consolas, Courier New, monospace"
                     FontSize="12"
                     Padding="8"
                     BorderThickness="0"
                     Background="Transparent"
                     Foreground="{DynamicResource TextBrush}"
                     CaretBrush="{DynamicResource TextBrush}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"
                     SpellCheck.IsEnabled="False"
                     x:Name="TokenBox"/>
        </Border>

        <TextBlock Grid.Row="3"
                   Text="{Binding ErrorMessage}"
                   FontSize="12"
                   Foreground="{DynamicResource DangerBrush}"
                   TextWrapping="Wrap"
                   Margin="0,8,0,0">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Cancelar"
                    Style="{StaticResource PrimaryButton}"
                    Background="{DynamicResource MutedBrush}"
                    MinWidth="100"
                    Margin="0,0,8,0"
                    Command="{Binding CancelCommand}"
                    IsCancel="True"/>
            <Button Content="Entrar"
                    Style="{StaticResource PrimaryButton}"
                    MinWidth="100"
                    Command="{Binding VerifyCommand}"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
